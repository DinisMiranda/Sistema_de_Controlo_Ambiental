# Server
PORT=3001
NODE_ENV=development

# MySQL (ajustar se usar Docker ou MySQL local)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=sistema_controlo_ambiental2
DB_DIALECT=mysql
SYNC_MODELS=false

# JWT (quando implementar autenticação)
# JWT_SECRET=your-secret-key
