# Optional: copy to .env
# This overrides the script default (20). Use -n on the command line to override .env.

NUM_UTILIZADORES=20

# Where to write the examination file (default: scripts/generated/utilizadores_examination.txt)
# UTILIZADORES_OUTPUT=/absolute/or/relative/path/to/file.txt
